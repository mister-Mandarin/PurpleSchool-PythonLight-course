# Спросить у пользователя имя и возраст последовательно.
# Сохранить результаты в переменные.
# Вывести строку: «Привет, <имя>! Тебе <возраст> лет». Называем скрипт - hello.py
condition: bool = False
name: str = ''
age: int = 0 

while not condition: 
    name = input("Введите ваше ИМЯ. Максимум 15 символов: ")
    if name.isalpha() and len(name) <=15:
        condition = True
    else: 
        print('Имя содержит недопустимые символы или слишком длинное. Повторите ввод.')
        continue

    age = input('Введите ваш ВОЗРАСТ. Сколько вам полных лет:')
    if age.isdigit() and 0 < int(age) < 100:
        condition = True
    else:
        print('Недопустимый возраст')
        continue

print(f'Привет, {name}! Тебе {age} лет.')

'''
🔍 Итоги прохождения ревью:

✅ Положительные моменты:

Все требования выполнены: программа последовательно запрашивает имя и возраст, сохраняет их в переменные и выводит в требуемом формате
Скрипт правильно назван hello.py и находится в директории 1-vars/
Добавлена валидация входных данных (проверка имени на буквы, возраста на цифры), хотя это не требовалось по заданию
Используются type hints для переменных, что улучшает читаемость кода
Применяется f-string для форматирования вывода
Добавлены информативные сообщения для пользователя при вводе данных
⚠️ Что можно улучшить:

В текущей реализации при ошибке валидации программа завершается (break), но всё равно выводит результат. Если хочешь, чтобы валидация работала корректно, замени break на continue - тогда цикл будет запрашивать данные заново при ошибке
'''